import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  DialogContent: {
    display: 'flex',
    justifyContent: 'center',
    overflow: 'hidden'
  }
}));

export { useStyles };
